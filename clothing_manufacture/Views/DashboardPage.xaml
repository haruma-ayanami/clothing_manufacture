<Page x:Class="clothing_manufacture.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource AppBackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0 0 0 14">
                <StackPanel>
                    <TextBlock Text="clothing_manufacture" Style="{StaticResource PageTitleStyle}"/>
                    <TextBlock Text="Информационная система для швейного производства. Проект подключается к SQL Server и работает с таблицами через Entity Framework Core." Style="{StaticResource PageDescriptionStyle}" Margin="0 6 0 0"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0 0 0 14">
                <StackPanel>
                    <TextBlock Text="Подключение" FontSize="20" FontWeight="SemiBold" Margin="0 0 0 10"/>
                    <TextBlock Text="{Binding ConnectionStatus}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Статистика таблиц" FontSize="20" FontWeight="SemiBold" Margin="0 0 0 10"/>
                    <ItemsControl ItemsSource="{Binding TableCounts}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="0 0 0 1" Padding="0 8">
                                    <TextBlock Text="{Binding}"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
